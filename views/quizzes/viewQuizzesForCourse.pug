html
    head
        title= title
        link(rel='stylesheet' href='https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css')
        link(rel='stylesheet' href='/css/style.css')
        script()
    body
        div.container
            h1 Listes des questionnaires
            if(quizzes.length > 0)
                table.table.table-hover.table-striped#dataTables(width='100%')
                    thead
                    tr
                        th Description
                        th Actions
                    tbody
                    each quiz, i in quizzes
                        tr
                        td= quiz.description
                        td 
                            a.btn.btn-info(href="/quiz/"+quiz.id, style='margin-bottom: 10px; margin-right: 10px') Voir
                            a.btn.btn-warning(href="/quiz/"+quiz.id+"/edit", style='margin-bottom: 10px; margin-right: 10px') Modifier
                            form(action="/api/v1/quiz/" + quiz.id + "?_method=DELETE" method="POST")
                                input(type="hidden" name="_method" value="DELETE")
                                button.btn.btn-danger(type='submit') Supprimer
                br
            else
             p  Aucun questionnaire disponible pour ce cours

            a.btn.btn-outline-primary(href="/quizzes") Retour à la liste de cours


    <script src="//code.jquery.com/jquery-2.2.4.min.js"></script>
    <script src="//stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
    <script type="text/javascript" src="/lib/main.js"></script>
